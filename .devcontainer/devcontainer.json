{
    "dockerFile": "Dockerfile",
    "updateRemoteUserUID": true,
    "containerUser": "dev",
    "remoteUser": "dev",
    "postAttachCommand": "go mod tidy",
    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go"
            ]
        }
    }
}